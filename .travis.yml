#install: gradle wrapper --gradle-version 5.0.1
language: android
dist: trusty
android:
     components:
          - tools
          - android-29
          - build-tools-29.0.2
      
      script:
        - chmod +x gradlew
        - ./gradlew build

